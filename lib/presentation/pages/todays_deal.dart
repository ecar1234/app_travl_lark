import 'package:flutter/material.dart';

class TodaysDeal extends StatelessWidget {
  const TodaysDeal({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("Todays Deal"),
    );
  }
}